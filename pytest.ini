[pytest]
addopts = --tb=short -q --import-mode=importlib
testpaths =
    tests
    portal/tests
norecursedirs =
    apps
    deployment
    web
    mobile
    models
    shared
    docs
    .github
    node_modules
    operator
    artifacts
    phase19/revenue_smoke_test
