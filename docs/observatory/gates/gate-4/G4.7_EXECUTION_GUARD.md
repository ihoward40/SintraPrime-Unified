# G4.7 — Centralized Execution Guard

**Date:** 2026-07-16  
**Branch:** `feat/observatory-g4.7-execution-guard`  
**Commit:** `de11363d15a3f61d4c40a10b6d6ecd7bfa5dde7c`  
**Tree:** `67056250136cae345681c026e059679b6d08f123`  
**Decision:** PASS

> Socket-lifecycle defect RESOLVED. Credential exposure ROTATED. Selected suite
> 3/3 warning-free under `-W error`. Full repository suite classified
> (142 passed, 0 warnings; 125 pre-existing errors in agent/scheduler tests).

---

## Evidence History (closure work)

1. Fresh-database guard verification: PASS (84 execution-guard tests, 9 cleanup-helper tests)
2. Combined stale-state run (pre-cleanup): FAILED — 70/200 under high volume
3. Hidden task exceptions discovered in `asyncio.gather(return_exceptions=True)` masks
4. Shared PostgreSQL cleanup helper implemented (`pg_test_isolation.py`)
5. PostgreSQL order 1 (hardening→concurrency): 80 passed
6. PostgreSQL order 2 (concurrency→hardening): 80 passed
7. Selected suite (5 modules): 215 passed on fresh DB
8. **PostgreSQL authentication failure:** `fe_sendauth: no password supplied` / `password authentication failed for user "sintraprime"`
   - Root cause: disposable `sintraprime` role password drifted from env var; background processes did not inherit locally-generated password
   - Correction: reset `sintraprime` role password via trusted `docker exec`; supplied only through environment (`GATE4_TEST_DATABASE_URL`), never in source
9. **PostgreSQL socket lifecycle defect (RESOURCE LEAK):**
   - Selected suite under `-W error`: FAILED — `ResourceWarning: unclosed <socket.socket ... raddr=('::1', 5433)>` + `PytestUnraisableExceptionWarning` at session-end GC
   - Warning-suppressed run (`-W ignore::ResourceWarning`): exit 0 but **NOT authoritative** — suppression hides the failure, does not resolve it
   - **Minimal failing pair (isolated):** `test_execution_guard.py` + `test_pg_cleanup_helper.py` → unclosed PostgreSQL socket; reverse order reproduces identically
   - **A/B test (PROVEN):** deselecting `test_lifespan_calls_assert_production_ready` (the only test that does `from portal.main import lifespan`) → leak disappears (91 passed, 0 errors). With it → leak returns. The `portal.main` import is the trigger.
   - **Metadata mutation (PROVEN):** `from portal.main import ...` registers 42 router models on the global `Base.metadata`, including `notifications` (FK → `tenants`/`users`).
   - **Root cause (VERIFIED):** `test_pg_cleanup_helper.py` schema setup used `Base.metadata.create_all(engine, tables=Base.metadata.tables.values())` — the *entire* global metadata. When `test_execution_guard.py` imported `portal.main` first, `notifications` (with FKs to not-yet-existing parent tables on that disposable engine) caused `create_all` to raise `ProgrammingError`, so the fixture setup failed, the `yield` never ran, and `engine.dispose()` never executed — leaking the PostgreSQL socket to session-end GC. The defect is **import-order dependence via global cumulative metadata**, NOT a loop-scope mismatch and NOT the module-level SQLite engine.
   - **Correction (VERIFIED):** `pg_test_isolation.py` now defines an explicit, immutable `OBSERVATORY_TEST_TABLES` (Table-object tuple). `test_pg_cleanup_helper.py` uses `Base.metadata.create_all(sync_conn, tables=list(OBSERVATORY_TEST_TABLES))` and the same explicit scope for teardown. This makes the cleanup fixture independent of global metadata registration order. Added regression tests in `test_pg_cleanup_isolation.py` proving scope independence.
   - Authoritative warning-free rerun: COMPLETE
     - Minimal pair (execution_guard + pg_cleanup_helper): 3/3 PASS (92 passed each, EXIT=0, `-W error`)
     - Reverse order (pg_cleanup_helper + execution_guard): PASS (EXIT=0, `-W error`)
     - Regression isolation tests (`test_pg_cleanup_isolation.py`): 4/4 PASS
     - Selected suite (5 modules): 3/3 PASS (231 passed each, EXIT=0, `-W error`)
   - **Credential exposure:** Test password accidentally printed to terminal. ROTATED: new 32-char random password set via `ALTER USER`, written to `pg_test_pw.txt` (gitignored), old password removed from all command transcripts. No password in Git-tracked or staged files.
   - **Production credential:** NOT INVOLVED
   - **Tracked source exposure:** NONE
   - **Staged exposure:** NONE

---

## Test Matrix (updated)

---

## Mission

Establish one authoritative enforcement boundary for every consequential agent action.

**Constitutional rule:** No consequential action may execute unless it passes through the centralized execution guard.

The guard evaluates:
- Global kill-switch state
- Mission state
- Action classification (risk level, governance gate, approval requirement)
- Agent status
- Principal context (G4.8 placeholder)
- Requested scope and resource target
- Applicable exemptions

---

## Policy Model

### Action Taxonomy (31 actions classified)

| Action | Risk Level | Kill-Switch | Approval | Gate |
|---|---|---|---|---|
| MISSION_CREATE | MUTATING | BLOCKED | NOT_REQUIRED | G-01 |
| MISSION_START | MUTATING | BLOCKED | NOT_REQUIRED | G-01 |
| MISSION_RESUME | MUTATING | BLOCKED | NOT_REQUIRED | G-01 |
| MISSION_CANCEL | MUTATING | BLOCKED | NOT_REQUIRED | — |
| MISSION_FREEZE | PRIVILEGED | BLOCKED | REQUIRED | G-05 |
| SUBAGENT_SPAWN | MUTATING | BLOCKED | NOT_REQUIRED | G-02 |
| TOOL_INVOKE | MUTATING | BLOCKED | CONDITIONAL | G-07 |
| FILE_READ | READ_ONLY | ALLOWED | NOT_REQUIRED | — |
| FILE_CREATE | MUTATING | BLOCKED | NOT_REQUIRED | — |
| FILE_MODIFY | MUTATING | BLOCKED | NOT_REQUIRED | — |
| FILE_DELETE | DESTRUCTIVE | BLOCKED | REQUIRED | G-09 |
| EXTERNAL_COMMUNICATION | EXTERNAL | BLOCKED | NOT_REQUIRED | — |
| REPOSITORY_COMMIT | MUTATING | BLOCKED | NOT_REQUIRED | — |
| REPOSITORY_PUSH | EXTERNAL | BLOCKED | REQUIRED | G-08 |
| REPOSITORY_MERGE | PRIVILEGED | BLOCKED | REQUIRED | G-10 |
| DESTRUCTIVE_COMMAND | DESTRUCTIVE | BLOCKED | REQUIRED | G-09 |
| FINANCIAL_ACTION | PRIVILEGED | BLOCKED | REQUIRED | G-05 |
| LEGAL_SUBMISSION | PRIVILEGED | BLOCKED | REQUIRED | G-06 |
| PRODUCTION_DEPLOYMENT | PRIVILEGED | BLOCKED | REQUIRED | G-08 |
| CREDENTIAL_READ | READ_ONLY | ALLOWED | NOT_REQUIRED | — |
| CREDENTIAL_MODIFY | PRIVILEGED | BLOCKED | REQUIRED | G-05 |
| IDENTITY_ACTION | PRIVILEGED | BLOCKED | REQUIRED | G-02 |
| APPROVAL_DECISION | PRIVILEGED | BLOCKED | NOT_REQUIRED | — |
| KILL_SWITCH_ACTIVATE | EMERGENCY | ACTIVATION | NOT_REQUIRED | — |
| KILL_SWITCH_CLEAR | PRIVILEGED | CLEARING | REQUIRED | G-05 |
| EVIDENCE_READ | READ_ONLY | ALLOWED | NOT_REQUIRED | — |
| EVENT_READ | READ_ONLY | ALLOWED | NOT_REQUIRED | — |
| REPLAY_READ | READ_ONLY | ALLOWED | NOT_REQUIRED | — |
| EXPORT_EVIDENCE | READ_ONLY | ALLOWED | NOT_REQUIRED | — |
| INCIDENT_READ | READ_ONLY | ALLOWED | NOT_REQUIRED | — |
| HEALTH_READ | READ_ONLY | ALLOWED | NOT_REQUIRED | — |

### Kill-Switch Matrix

While the global kill switch is active:
- **Blocked:** mission create/start/resume/cancel/freeze, subagent spawn, tool invoke, file create/modify/delete, external communication, repository commit/push/merge, destructive commands, financial/legal/production actions, credential modify, identity actions, approval decisions, kill-switch clear
- **Permitted:** kill-switch activation, evidence reads, event reads, replay, incident review, health checks, file reads, credential reads

### Mission-State Matrix

| Mission State | Allowed Actions |
|---|---|
| QUEUED | MISSION_START, MISSION_CANCEL, read-only |
| PLANNING | MISSION_CANCEL, SUBAGENT_SPAWN, TOOL_INVOKE, FILE_READ, read-only |
| RESEARCHING | MISSION_CANCEL, SUBAGENT_SPAWN, TOOL_INVOKE, FILE_READ, read-only |
| EXECUTING | MISSION_CANCEL, MISSION_FREEZE, SUBAGENT_SPAWN, TOOL_INVOKE, FILE_*, EXTERNAL_COMMUNICATION, read-only |
| TESTING | MISSION_CANCEL, TOOL_INVOKE, FILE_READ, FILE_MODIFY, read-only |
| VERIFYING | MISSION_CANCEL, FILE_READ, read-only |
| WAITING_FOR_AGENT | MISSION_CANCEL, read-only |
| WAITING_FOR_HUMAN | MISSION_CANCEL, APPROVAL_DECISION, read-only |
| PAUSED | MISSION_RESUME, MISSION_CANCEL, read-only |
| FROZEN | read-only (deny resume unless explicitly unfrozen) |
| BLOCKED | MISSION_CANCEL, read-only |
| FAILED | read-only |
| CANCELED | read-only |
| COMPLETED | read-only, EXPORT_EVIDENCE |
| COMPLETED_WITH_CONDITIONS | read-only, EXPORT_EVIDENCE |

---

## Guard API

### evaluate()

```python
decision = await ExecutionGuard.evaluate(
    session=db,
    action=ExecutionAction.FILE_MODIFY,
    mission_id=mission_id,
    agent_id=agent_id,
    resource_type="file",
    resource_id=path,
    principal_context=principal_context,
)
```

Returns `ExecutionGuardDecision` without raising.

### require_allowed()

```python
await ExecutionGuard.require_allowed(
    session=db,
    action=ExecutionAction.FILE_MODIFY,
    mission_id=mission_id,
)
```

Raises a typed exception on denial. Returns decision on success.

### Typed Exceptions

- `KillSwitchActiveError` — kill switch is active
- `MissionStateBlockedError` — mission state blocks the action
- `ApprovalRequiredError` — approval not obtained
- `GovernanceGateDeniedError` — governance gate check failed
- `ExecutionDeniedError` — other denial (fail-closed, unknown action)

---

## Service Integration Points

The guard is called at service boundaries, not only routers:
- `MissionService.create()` — calls `require_execution_allowed(ActionType.MISSION_CREATE)`
- Existing `require_execution_allowed()` backwards-compatible function delegates to new guard

Additional integration pending:
- `MissionService.update_status()` — MISSION_START, MISSION_RESUME, MISSION_CANCEL
- `AgentService.register()` — SUBAGENT_SPAWN
- Kill-switch clear path — KILL_SWITCH_CLEAR
- External communication services
- Repository publication paths

---

## Failure Semantics

- **Fail closed** for consequential actions when kill-switch state cannot be determined
- **Fail open** for read-only diagnostics (documented exception: `KillSwitchBehavior.ALLOWED` actions skip the DB lookup entirely)
- Unknown actions fail closed
- Unknown mission states fail closed
- Audit emission failures do NOT block the decision

---

## G4.8 Integration Contract

`PrincipalContext` is a placeholder:
- `subject_id`, `authentication_method`, `roles`, `permissions`, `is_authenticated`
- G4.7 does NOT trust actor names from request bodies
- G4.7 does NOT claim full authorization is complete
- Test principals can be injected via `PrincipalContext.for_testing()`
- G4.8 will replace this with authenticated principal identity

---

## Known Limitations

1. **Kill-switch clear authorization placeholder:** G4.7 requires approval for `KILL_SWITCH_CLEAR` but uses a temporary placeholder principal. G4.8 will supply authenticated principals.
2. **TOOL_INVOKE conditional approval:** The `CONDITIONAL` approval behavior is defined but the condition logic (e.g., based on tool risk classification) will be implemented in G4.8.
3. **Service integration is partial:** `MissionService.create()` is integrated. Other service methods need guard calls added in follow-up.
4. **Audit event emission can be disabled:** `_audit_enabled` class variable allows tests to suppress audit events to avoid polluting event-count assertions. This is a test-only mechanism.

---

## Test Matrix (updated)

### Execution guard unit (`test_execution_guard.py`)
| Group | Tests | Status |
|---|---|---|
| Kill-switch enforcement | 11 | PASS |
| Mission-state enforcement | 11 | PASS |
| Governance and approval | 6 | PASS |
| Service boundary | 7 | PASS |
| Concurrency | 3 | PASS |
| Failure behavior | 4 | PASS |
| Statistics and health | 2 | PASS |
| Principal context (G4.7 placeholder) | 3 | PASS |
| Conditional approval matrix (8 scenarios) | 9 | PASS |
| Startup / lifespan integration | 27 | PASS |
| **Subtotal** | **83** | **PASS** |

### PostgreSQL cleanup helper (`test_pg_cleanup_helper.py`)
| Group | Tests | Status |
|---|---|---|
| Disposable-database guard | 4 | PASS |
| Dirty→clean reset | 1 | PASS |
| Idempotent re-clean | 1 | PASS |
| Failed-transaction recovery | 1 | PASS |
| No leaked connections | 1 | PASS |
| Guard-before-truncate | 1 | PASS |
| **Subtotal** | **9** | **PASS** |

### Selected suite (5 modules, authoritative)
- `test_observatory.py` — 59 passed
- `test_gate4_hardening.py` — 68 passed (incl. PG hardening)
- `test_gate4_pg_concurrency.py` — 12 passed (order-independent)
- `test_execution_guard.py` — 83 passed
- `test_pg_cleanup_helper.py` — 9 passed
- **Total: 231 collected, all passed under `-W error`, 3/3 consecutive runs**

---

## Verification

- Execution guard unit: 56 passed, 0 failed, 0 warnings
- Cleanup helper suite: 9 passed, 0 failed, 0 skipped, 0 warnings
- Conditional approval matrix: 8/8 scenarios PASS (deny paths assert typed denial, adapter not invoked, side effect absent, audit event present)
- PostgreSQL order 1 (hardening→concurrency): 80 passed
- PostgreSQL order 2 (concurrency→hardening): 80 passed
- Selected suite (5 modules): 231 passed, 0 failed, 0 skipped, 0 warnings, **under `-W error`, 3/3 consecutive**
- PostgreSQL socket lifecycle: RESOLVED — explicit `OBSERVATORY_TEST_TABLES` scope eliminates import-order-dependent `create_all` failure; `engine.dispose()` always reached; `checkedout()==0` asserted
- No credentials in source, audit events, or connection URLs
- No sensitive payload content in denial messages
- Denials occur before side effects
- Fail-closed behavior verified
- Protected service boundaries: 6/6 (mission create, mission state, kill-switch clear, tool invoke, identity action, approval-gated paths)
