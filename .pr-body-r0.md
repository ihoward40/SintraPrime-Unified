# R0 — SintraPrime Foundation Reality Audit and Certification

Repository Reality Audit per the revised master directive (REALITY BEFORE CAPABILITY). **DRAFT ONLY — evidence, no merge.**

---

## What This PR Is

An **audit increment**, not a feature increment. It establishes repository truth: what actually works, what is partial, broken, disconnected, docs-only, or missing — with runtime evidence, not documentation claims.

No production code paths were modified. Added: audit artifacts + `sintraprime_doctor` diagnostics tool.

## Evidence Artifacts (`artifacts/repository-reality/`)

| Artifact | Purpose |
|---|---|
| `SINTRAPRIME_CAPABILITY_REALITY_LEDGER.md` | Every major capability classified (VERIFIED_WORKING / PARTIALLY_WORKING / BROKEN / DISCONNECTED / STUB_ONLY / DOCS_ONLY / MISSING) with code locations and evidence |
| `SINTRAPRIME_INTEGRATION_GRAPH.md` | Working/broken/disconnected edges |
| `TEST_REALITY_MATRIX.md` | What tests actually run, on what DB, with what confidence |
| `PROVIDER_RELIABILITY_MATRIX.md` | Provider adapter status (0 real providers connected) |
| `SINTRAPRIME_DEFICIENCY_REGISTER.json` | 18 findings (SP-DEF-001..018) with severity |
| `FOUNDATION_READINESS_SCORECARD.md` | Test-derived subsystem scores (~45% overall) |
| `SMOKE_RECEIPT.json` | Commit-specific machine-readable receipt |
| `portal/scripts/sintraprime_doctor.py` | Diagnostics tool (directive §13) |

## Headline Findings

### VERIFIED WORKING
- App startup: 197 routes, health endpoints
- Auth: JWT enforcement (401 unauthenticated), RBAC permission checks (403 unauthorized)
- Correlation IDs: generated on every request
- **Message persistence on PostgreSQL**: 10/10 parallel inserts, ordered retrieval, restart persistence, cross-tenant FK rejection
- Default CI lane (Tier 1): 556/563 pass (7 failures are environment PG credentials)

### BROKEN (P1)
- **SP-DEF-001** — MissionControlCommand `tenant_id`: ORM `String(36)` vs SQL `UUID` → PG rejects
- **SP-DEF-002** — Message ORM `body` vs SQL `content` → PG rejects application writes
- **SP-DEF-003** — Migration chain ordering broken (6 relation-not-exists errors)
- **SP-DEF-005** — Alembic not wired (0 versions)
- **SP-DEF-012** — 2 stale portal test imports

### DISCONNECTED
- GovernanceEngine: zero runtime callers
- Event hub (`core/universe/event_hub/`): zero importers
- Channels (Slack/Discord): zero runtime integration
- Multi-tenant governance: only called by simulation scripts

### STUB_ONLY
- Provider registry: **5 mock providers, 0 real** — no LLM invocation possible
- Orchestrator: in-memory RUNS dict, no persistence
- Outbox: model exists, no service

### MISSING
- Workflow runtime (not on origin/main)
- OmniBrain (docs only)
- Smoke runner (added `sintraprime_doctor` as starting point)
- Run control proof (models exist, unverified)

### Docs-Only
- OmniBrain

## Dynamic Proofs Executed

1. **Startup**: `portal.main` imports, 197 routes ✅
2. **Auth**: `/health` 200 unauthenticated; `/api/system/health` 401 unauthenticated, 200 authenticated ✅
3. **RBAC**: MC commands → 401 no token, 403 wrong permission, authenticated SUPER_ADMIN reaches DB layer ✅
4. **MC command on PG**: ❌ UUID drift (SP-DEF-001) — reaches service, fails at INSERT
5. **Message persistence on PG**: ✅ 10/10 parallel, ordered, restart-persistent, cross-tenant rejected
6. **Migrations clean bootstrap**: ❌ ordering (SP-DEF-003)
7. **Agent execution**: NOT RUN — no harness exists
8. **Provider invocation**: BLOCKED — no real providers connected

## Recommended R1 Scope (for Principal decision)

1. **R1A Persistence**: Fix SP-DEF-001/002/003/005 — align ORM↔schema, migration ordering, alembic, PG default
2. **R1B Security**: JWT key length (SP-DEF-011), PG tenant isolation certification
3. **R1D Messaging**: Outbox service (SP-DEF-013), event hub integration (SP-DEF-010)
4. **R1E Providers**: Real provider adapters + reliability matrix
5. **R1G Mission Control**: MC command E2E on PG after R1A
6. **Testing**: Unlock Tier 2 lane, fix stale imports (SP-DEF-012)

## Gates Assessment

| Gate | Status |
|---|---|
| F0 Repository Boot | PARTIAL — app boots, no production PG default |
| F1 Persistence | FAIL — schema/model drift, migration ordering |
| F2 Security/Tenant | PASS (auth+RBAC+FK tenant isolation verified) |
| F3 Audit | PARTIAL — correlation works, audit table ordering bug |
| F4 Messaging/Events | PARTIAL — message persistence verified, outbox/event hub not integrated |
| F5 Providers | FAIL — mock-only |
| F6 Agents | FAIL — no execution harness |
| F7 Mission Control | FAIL — ledger can't write on PG |
| F8-F10 | NOT OPERATIONAL |

## Explicit Confirmations

NO MERGE
NO DEPLOY
NO PRODUCTION ACTIVATION
NO CONSEQUENTIAL EXTERNAL ACTION
PRINCIPAL DECISION GATE — R1 does not begin without Principal approval
