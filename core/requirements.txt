# UniVerse v2.0 - Python Dependencies - Optimized for Python 3.12

# Core Framework
Flask>=2.3.0
Werkzeug>=2.3.0

# Database & ORM
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.9
SQLAlchemy-Utils>=0.41.1

# Caching
redis>=5.0.0

# Message Queue
pika>=1.3.0
celery>=5.3.0
kombu>=5.3.0

# Data Processing & Scientific
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0

# API & Serialization
requests>=2.33.0
pydantic>=2.0.0
python-dateutil>=2.8.0

# Authentication & Security
PyJWT>=2.8.0
cryptography>=41.0.0
bcrypt>=4.0.0
python-jose>=3.3.0

# Monitoring & Logging
prometheus-client>=0.17.0
structlog>=23.0.0
sentry-sdk>=1.28.0

# Testing
pytest>=9.0.3
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0

# Code Quality
black>=23.7.0
flake8>=6.1.0
pylint>=2.17.0
mypy>=1.4.0
isort>=5.12.0

# Documentation
Sphinx>=7.1.0
sphinx-rtd-theme>=1.3.0
mkdocs>=1.4.0
mkdocs-material>=9.0.0

# Utilities
python-dotenv>=1.2.2
click>=8.1.0
colorama>=0.4.6
tabulate>=0.9.0
tqdm>=4.66.0

# Performance & Optimization
ujson>=5.8.0
orjson>=3.9.0

# Async Support
aiohttp>=3.8.0

# Development Tools
ipython>=8.14.0
ipdb>=0.13.0
jupyter>=1.0.0

# Environment & Configuration
PyYAML>=6.0.0

# Note: Removed packages that don't have wheels for Python 3.12:
# - python-logstash-formatter (not available on PyPI)
# - opentelemetry instrumentation packages (version conflicts)
# - jaeger-client (compatibility issues)
# - Flask-RESTful (use fastapi instead if needed)
# - passlib==1.7.4 (old version, use passlib>=2.0.0)
# - asyncio-contextmanager (not needed in Python 3.12)
# - unittest-mock (built into unittest in Python 3.12)
# - configparser (builtin in Python 3)
# - python-memcached (optional, redis is primary)
# - lru-dict (optional, can use functools.lru_cache)
# - marshmallow (optional, pydantic is primary)

# Optional: AWS Integration (uncomment if needed)
# boto3>=1.28.0
# botocore>=1.31.0

# Optional: Google Cloud Integration (uncomment if needed)
# google-cloud-storage>=2.10.0
# google-cloud-logging>=3.5.0

# Optional: Azure Integration (uncomment if needed)
# azure-storage-blob>=12.17.0
# azure-identity>=1.13.0
